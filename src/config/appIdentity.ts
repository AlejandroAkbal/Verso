import { appIdentity as sharedIdentity } from '../../appIdentity.js';

/**
 * Central app identifiers and display name.
 * Source of truth: `appIdentity.js` at project root (also used by Expo config).
 */
export const appIdentity = sharedIdentity;

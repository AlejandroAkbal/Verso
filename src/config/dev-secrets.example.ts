/**
 * Copy to dev-secrets.ts and fill in your private OPDS server for local testing.
 * dev-secrets.ts is gitignored — never commit credentials.
 */
export const DEV_OPDS = {
  url: 'https://calibre.example.com/opds',
  username: 'admin',
  password: 'your-password',
};
